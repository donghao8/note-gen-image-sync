# 上下文退化：增加输入Tokens对大型语言模型性能的影响

大型语言模型（LLMs）通常被认为能够以统一的方式处理上下文，即模型应当能够像处理第100个标记那样可靠地处理第10,000个标记。然而，在实际应用中，这一假设并不成立。我们观察到，即使在简单任务上，随着输入长度的变化，模型性能也会发生显著波动。

在本报告中，我们评估了18种大型语言模

型，包括最先进的GPT-4.1、Claude 4、Gemini 2.5和Qwen3模型。研究结果表明，这些模型并未均匀利用其上下文；相反，随着输入长度的增加，其性能变得愈发不可靠。

![Claude Sonnet 4, GPT-4.1, Qwen3-32B, and Gemini 2.5 Flash 在重复词汇任务上的表现](https://fastly.jsdelivr.net/gh/bucketio/img19@main/2025/08/28/1756358303650-1e708d20-8be8-4285-8a9a-f428fb1e1b3a.png)

近期大型语言模型（LLMs）的发展显示出上下文窗口逐渐延长的趋势，最新模型的输入标记数量已达到数百万。由于这些模型在广泛采用的基准测试中（如“大海捞针”（Needle in a Haystack, NIAH）[1]）取得了接近完美的分数，因此通常认为它们在长上下文任务中的表现是均匀一致的。

然而，NIAH本质上是一项简单的检索任务，其中将一个已知句子（“针”）插入到一段与其无关的长文本（“草堆”）中，模型被要求检索出该句子。尽管这一基准具有可扩展性，但它通常评估的是直接的词汇匹配能力，这可能无法代表灵活且以语义为导向的任务。

![示例：大海捞针（NIAH）设置](https://fastly.jsdelivr.net/gh/bucketio/img7@main/2025/08/28/1756358236974-849b6ca2-135b-4e6e-a3b2-0b94bcaa60b5.png)

我们扩展了标准的NIAH任务，以研究模型在先前未充分探索的环境中的行为。我们考察了具有语义匹配而非直接词汇匹配的“针”所产生的影响，同时还分析了对“干草堆”内容引入变化所带来的效果。

此外，我们还包含了一个基于LongMemEval [2] 的对话式问答评估，以及一个要求模型重复一系列重复词语的合成任务。每个任务均刻意保持简单，并经过精心控制，以单独隔离上下文长度的影响。

我们证明，即使在这些最简条件下，随着输入长度的增加，模型性能仍会出现下降，且往往以令人惊讶和不均匀的方式表现。现实世界中的应用通常涉及更大的复杂性，这意味着输入长度的影响在实践中可能更为显著。

我们的深度技术报告如下。如果您认为我们的工作对您有所帮助，请考虑引用我们的研究。

# 1、介绍

现代大型语言模型（LLMs）通常具备处理数百万个标记的输入上下文长度。Gemini 1.5 Pro [3] 在2024年初首次引入了其100万标记的上下文窗口，随后是近期发布的GPT-4.1的100万标记上下文窗口 [4] 和Llama 4的1000万标记上下文窗口 [5]。长上下文的应用场景极具吸引力：更长的上下文意味着大型语言模型在每次调用时能够处理更多信息，并生成更具信息量的输出。

对这些模型的长上下文评估通常显示其在不同输入长度上的性能保持一致。然而，这些评估范围狭窄，无法充分反映长上下文在实际应用中的使用情况。最常用的测试方法“大海捞针”（Needle in a Haystack, NIAH）是一项简单的词汇检索任务，常被用来泛化模型可靠处理长上下文的能力。然而，诸如代理任务或摘要生成等实际应用场景需要对更广泛、通常更模糊的信息进行显著更多的处理和推理。

设计现实的长上下文基准测试颇具挑战性。随着输入长度增加，任务复杂性往往也随之提高，这使得难以区分性能下降是由于输入变长还是问题本身变得更难。为了解决这一问题，我们的实验在保持任务复杂性不变的情况下仅调整输入长度，从而能够直接测量输入长度单独带来的影响。

# 2、贡献

我们展示了以下内容：

1. 对包括领先的闭源模型和开源权重模型在内的18个大语言模型（LLMs）进行评估，揭示了随着输入长度增加，性能表现的非均匀性。
2. 关于模型在处理干扰项及不同问答相似度时所表现出的特定行为模式的详细分析。
3. 用于复现我们研究结果的完整代码库。

# 3、相关工作

评估模型长上下文能力时最广泛使用的基准之一是“大海捞针”（Needle in a Haystack，简称NIAH）。尽管NIAH作为一种可扩展的测试具有一定的实用性，但它仅衡量了一种狭窄的能力：词汇检索。模型通常在NIAH测试中表现良好，这导致了一种普遍的认知，即长上下文问题已基本得到解决。

然而，NIAH低估了大多数长上下文任务在实际应用中的需求。NIAH的变体，例如包含非词汇匹配的针-问题对的NoLiMa [6]，揭示了模型性能的显著下降。其他看似难度相似的任务，例如测试模型识别给定文本片段缺失的AbsenceBench [7]，也表明随着输入长度的增加，模型性能会出现退化。

此外，长上下文任务通常涉及在任务中对干扰项进行消歧。一个典型的例子是多轮共指解析（Multi-round co-reference resolution，简称MRCR）[8] [9]，该任务要求在多轮对话中检索特定用户提问的第i个实例，同时需要区分与之相似的其他用户提问。然而，目前对于干扰项在长上下文环境中的影响仍缺乏深入研究。

长上下文任务中的一个重要因素是如何扩展输入长度。Latent List [8] 是一项任务，要求模型在不同输入长度下执行固定数量的Python列表操作。通过测试填充无关上下文的不同方式，可以发现其对模型性能的影响是非均匀的 [1]。例如，添加局部相互抵消的列表操作比添加打印语句对模型性能的负面影响更为显著。这表明“无关内容”的类型至关重要，因为某些无关内容可能会随着输入长度的增加引入更高的复杂性。

类似地，Graphwalks [10] 是一项图遍历任务，模型被提供一个由十六进制哈希值组成的有向图，并被要求从随机节点开始执行广度优先搜索。输入长度的增加意味着需要遍历的图规模增大，从而导致任务难度上升。在这种情况下，很难将任务复杂性的增加与输入长度区分开来，这也使得单独评估输入长度对性能的影响变得困难。这一点突显了将输入长度作为研究变量的重要性，这对于理解大语言模型（LLMs）在处理长输入时的实际行为至关重要。

# 4、大海捞针扩展

经典的“大海捞针”任务涉及将一个随机事实（“针”）置于一个较长的上下文窗口（“干草堆”）中间，然后要求模型回答与该事实相关的问题。

此任务的原始实现使用了一对带有词法匹配的“针-问题”组合。然而，在实践中使用长上下文通常需要对模糊任务进行语义理解。

![具有词法匹配的大海捞针（NIAH）示例设置](https://fastly.jsdelivr.net/gh/bucketio/img9@main/2025/08/28/1756360132204-0b6e1ad2-0776-4fea-a4f3-8f502ae5f985.png)

NoLiMa 已经证明，随着上下文长度的增加，非词汇匹配对模型来说是一个挑战。该任务利用了针-问题对（needle-question pairs），要求模型推断潜在的关联关系，例如：

```
问题：哪个角色去过Helsinki？
"针"：实际上，Yuki 住在Kiasma博物馆旁边。
```

为了回答这个问题，模型首先必须知道Kiasma博物馆位于Helsinki，然后建立这种潜在的关联链接。这不仅测试了模型的非词汇匹配能力，还测试了其世界知识。来自NoLiMa的72.4%的“针-问题”对需要这样的外部知识，使得这一基准更接近于测试模型如何同时处理这两项任务，而不仅仅是纯粹的非词汇匹配。

孤立测试非词汇匹配的影响仍然是一个尚未充分探索的领域。此外，这种“词汇”与“非词汇”的二元区分过于简化了现实世界场景中问答任务的复杂性。“针-问题”对实际上处于相似性的连续谱上，但它们却被归类到这些宽泛的类别中。

模型通常还需要应对干扰项，这已被证明会降低性能 [11]。

在本报告中，我们区分了干扰项和无关内容：

![比较——干扰因素vs.无关语境](https://fastly.jsdelivr.net/gh/bucketio/img9@main/2025/08/28/1756360847469-370a0264-94f5-47b8-bf05-9ac814ce9592.png)

- 干扰项与目标主题相关，但并不能准确回答问题。
- 无关内容则与目标和问题均无关联。

以往的研究表明，干扰项的影响是非均匀的，但大多数评估涉及的输入长度较短，并基于较旧的模型。当前最先进的模型被认为对干扰项更具鲁棒性，但其性能尚未在不同输入长度下进行广泛测试。

NIAH中另一个未被充分探索的方面是 “大海捞针” 本身，它通常仅被视为扩展输入长度的一种手段，但这假设了“大海捞针” 的内容本身对任务性能没有影响。如果模型确实对 “大海捞针”的内容不敏感，那么改变这些内容，例如“大海捞针”的主题或叙事流程，应该不会对结果产生影响。然而，这一假设在很大程度上仍未得到验证。

我们设计了四项受控实验，以研究这些因素的影响：

## 4.1 Needle-Question相似

我们通过嵌入向量计算“针-问题”对之间的余弦相似度。为确保稳健性，我们对五种嵌入模型的结果取平均值，这些模型包括：text-embedding-3-small、text-embedding-3-large、jina-embeddings-v3、voyage-3-large 和 all-MiniLM-L6-v2。我们进一步评估了随着输入长度增加，模型性能如何受到针-问题相似度的影响。

### 4.1.1 干扰物的影响

选取一个高相似度的“针-问题”对，我们编写了四个干扰项。实验设置如下：

- **基线**：仅包含“针”，无干扰项
- **单一干扰项**：包含“针”和一个随机位置的干扰项
- **多干扰项**：包含“针”以及所有四个随机排列的干扰项

我们通过增加输入长度，测试干扰项对模型性能的影响，以衡量干扰项之间的非均匀性及输入长度的影响。

## 4.2 Needle-Haystack相似

我们使用了两个主题迥异的“信息库”（haystacks），分别是保罗·格雷厄姆的文章和arXiv论文[12]，并为每个信息库编写了相应的“针”（needle）。为了测量“针”与“信息库”的相似性，我们将信息库嵌入向量空间，并针对每个“针”检索出最相似的五个片段，然后计算其余弦相似度得分的平均值。为确保结果的稳健性，这一过程在五种不同的嵌入模型中重复进行。

### 4.2.1 信息库的结构

在典型的NIAH（ Needle In A Haystack ）设置中，信息库是由连贯的文本串联而成的，每段文本都有其自身的逻辑思路。例如，最初的NIAH基准测试使用了一系列保罗·格雷厄姆的文章，其中每篇文章都遵循一种结构化的思想组织方式以形成论点。为了评估这种结构是否影响模型性能，我们比较了两种条件：

- 原始顺序：保留每个摘录中的自然思想流程。
- 打乱顺序：句子在整个信息库中被随机重新排序，以保持相同的总体主题但无逻辑连续性。

我们得出以下结论：

- 在所有实验中，随着输入长度的增加，模型性能始终下降。
- 相似度较低的针-问题对会加速性能下降的速度。
- 干扰项对模型性能的影响是非均匀的，这取决于它们彼此之间的干扰程度。随着输入长度增加，这种影响变得更加显著，并且我们观察到不同模型对干扰项的响应存在差异。
- 针-信息库相似性对模型性能的影响并不一致，表明需要进一步研究。
- 信息库的结构性模式始终显示出对模型处理长输入的方式产生影响。

### 4.2.2 细节

对于每种针类型、信息库主题和信息库结构的独特组合，我们对每个模型进行以下测试：

- 8种输入长度
- 11种针位置

除非特定设置不兼容（例如 o3）或明确不推荐使用（例如通义千问的“思考模式”），否则我们会在温度参数为0的情况下，在模型的最大上下文窗口内对其进行评估。对于通义千问模型，我们采用YaRN方法 [13] 将上下文长度从32,768扩展至131,072个标记。

在适用的情况下，我们同时评估模型的标准模式和“思考模式”。

我们使用与GPT-4.1对齐的评判模型对输出结果进行评估，并采用附录中概述的方法。

我们注意到，在极少数情况下，模型会拒绝执行任务（在总计194,480次大语言模型调用中发生了69次——占比0.035%）。例如，Claude Opus 4有时可能会因stop_reason=”refusal”而产生空输出。

# 5、Needle-Question相似

在现实世界的应用中，模型通常需要处理模糊的任务，并在不依赖精确词汇匹配的情况下识别相关信息。例如，当一个智能体被赋予一项涉及大规模语料库检索的任务时，用户很少会提供与相关内容精确对应的关键词。相反，模型必须推断出信息的相关性。我们通过嵌入向量的余弦相似度来量化“针-问题”对的相似性，并对其进行变化。

我们发现，随着“针-问题”相似性的降低，模型性能会随着输入长度的增加而显著下降。这反映了更加真实的场景：在这些场景中，精确的问题-答案匹配很少见，而语义上的模糊性进一步加剧了处理长输入的挑战。

## 5.1 实验

我们从两个领域获取“干草堆”内容：保罗·格雷厄姆（Paul Graham）的文章（如原始的NIAH实验中所用）和arXiv论文。对于每个“干草堆”主题（PG文章、arXiv），我们首先确定常见主题，以指导我们设计问题和编写“针”内容。我们使用聚类方法来识别给定语料库中最常见的主题：

1. 将文档分割为1至3句话的片段；
2. 使用text-embedding-3-large对每个片段进行嵌入编码；
3. 使用UMAP [14] 进行降维，参数设置如下：n_neighbors=30, min_dist=0.05, n_components=50, random_state=42；
4. 使用HDBSCAN [15] 创建聚类簇，参数设置如下：min_cluster_size=10, min_samples=15；
5. 使用最大边际相关性（MMR）提取最大聚类簇中的20个代表性片段；
6. 手动检查最大的聚类簇，以确定其主题和风格。

通过这种方法，我们发现写作建议是PG文章中的常见主题，通常以轶事形式呈现。对于arXiv论文，我们发现信息检索是一个常见主题，特别是重排序（re-ranking）相关内容。

针对每个主题，我们编写了相应的问题：

```
关于Paul Graham论文和arXiv论文的问题

PG散文：“我从大学同学那里得到的最好的写作建议是什么？”
arXiv论文：“哪个低延迟重新排序器更适合科学领域？”
```

在撰写我们的“针”之前，我们验证这些问题的答案并不存在于“干草堆”内容中：

1. 我们将先前计算的干草堆片段嵌入存储在一个向量数据库中。
2. 使用我们的问题嵌入从该向量数据库中查询前10个结果。
3. 手动检查这些结果以确认它们并未回答给定的问题。

这构建了一个公平的测试环境，因为它确保了替代答案不存在，任何错误答案均归因于模型的幻觉现象。

对于每个问题，我们编写8个“针”，这些“针”均属于通过近似预测验证后的大类集群。“针”如果以大于0.9的概率归属于写作/检索集群，则被认为在主题上与干草堆融为一体。我们手动编写这些“针”以避免数据污染。

对于这8个“针”，我们也调整了模糊程度，并通过以下方法对其进行量化：

1. 使用嵌入模型，我们计算“针”和问题的嵌入及其余弦相似度。
2. 在五个嵌入模型中重复此过程（text-embedding-3-small、text-embedding-3-large、jina-embeddings-v3、voyage-3-large 和 all-MiniLM-L6-v2）。

对于PG论文主题，我们的“针”与问题之间的相似度范围为0.445至0.775，在五个嵌入模型中的标准差小于0.1。对于arXiv主题，“针”与问题之间的相似度范围为0.521至0.829，同样具有小于0.1的标准差。

## 5.2 结果

我们观察到一个明显的模式，即在输入长度方面，性能随着针-问题对的相似性降低而下降得更快。

![NIAH：针-问题相似性（同一模型的思考/非思考模式被分开处理）- arXiv干草堆/arXiv针
高性能：性能超过33%
蓝色：高相似度针（相似度在50%以上）
红色：低相似度针头（相似度低于50%）](https://fastly.jsdelivr.net/gh/bucketio/img10@main/2025/08/28/1756363887935-fab6b7bf-0688-433c-9aba-c419fe382de1.png)

在较短输入长度下，即使对于相似度较低的配对，模型的表现依然良好。这一点在高性能和中性能模型中表现得尤为明显，表明这些模型能够成功处理所有针-问题配对。

在较长输入长度下观察到的性能下降并非由于针-问题配对本身的固有难度所致。通过固定针-问题配对并仅改变无关内容的数量，我们将输入规模确定为导致性能下降的主要因素。

我们还考察了针的位置是否会对性能产生影响。通过对11个针位置进行测试，我们发现，在这一特定的NIAH任务中，性能并未出现显著变化。

## 5.3 干扰物的影响

已有研究通过较旧的模型证实，干扰项会降低模型性能，并且其影响是非均匀的。虽然有声称指出新型模型能够可靠地处理任何干扰项，但随着输入长度的增加，这一说法是否仍然成立？

我们的实验表明，包括最新的最先进模型在内，随着输入长度的增长，干扰项的影响及其非均匀性会逐渐放大。我们还观察到不同模型系列在处理歧义时表现出显著的行为差异。

## 5.4 实验

从每个“干草堆”主题（PG文章和arXiv论文）中，我们选取与问题相似度较高的“针”（在八个选项中排名第二高），并手动编写四个干扰项：

```
高问题相似度的保罗·格雷厄姆论文主题和针的干扰因素

问题：“我从大学同学那里得到的最好的写作建议是什么？”
答案：“我认为我从大学同学那里得到的最好的写作建议是每周写作。”
干扰项：
  - “我从大学教授那里得到的最好的写作建议是每天写作。”
  - “我从大学同学那里得到的最差的写作建议是用五种不同的风格写每篇论文。”
  - “我从同学那里得到的最好的写作建议是用三种不同的风格写每篇论文，那还是在高中时。”
  - “我原以为我从大学同学那里得到的最好的写作建议是用四种不同的风格写每篇论文，但现在不这么认为了。”
```

我们并没有使用所有八根针及其干扰项进行测试，而是选择了一根与问题相似度较高的针，以此构建一种相对容易识别针的条件。从先前的结果中可以看出，由于针与问题之间的高相似性，模型在不同输入长度下通常都能较好地识别这根针。这使得我们能够更好地单独隔离并测量干扰项的影响。

我们进行了三种测试条件：

- 无干扰项（基线）：仅包含针
- 单个干扰项：针 + 一个干扰项（随机位置）
- 多个干扰项：针 + 所有四个干扰项，随机分布于整个“干草堆”中

![干扰物的影响——三种情况](https://fastly.jsdelivr.net/gh/bucketio/img8@main/2025/08/28/1756364409887-14e286cc-6e2c-4046-8bcf-daac334a765c.png)

## 5.4 结果

即使只有一个干扰项，其性能相对于基线（仅针）也会有所下降，而增加四个干扰项则会进一步加剧这种性能的下降。

![干扰物的影响：干扰物数量的表现- arXiv/PG论文针](https://fastly.jsdelivr.net/gh/bucketio/img9@main/2025/08/28/1756374078095-dfda98e3-0408-4001-b9c9-81f6130ebd66.png)

我们还能够观察到，干扰项的影响并非一致。例如，在我们的arXiv文档库与PG论文针组合中，可以看到干扰项3（红色）相较于其他干扰项导致了更大的性能下降。

![干扰物的影响：个体干扰物的表现- arXiv干草堆/PG论文针](https://fastly.jsdelivr.net/gh/bucketio/img15@main/2025/08/28/1756374153207-acb8a71b-47e0-4411-94f7-9b7fa85b5dfe.png)

为了进一步研究这种非均匀影响，我们分析了在4个干扰项条件下不同模型的失败尝试。对于arXiv文档库与PG论文针组合，我们发现干扰项2和干扰项3在各模型生成的幻觉响应中出现频率最高。

![干扰因素的影响：失败分析- arXiv干草堆/PG论文针](https://fastly.jsdelivr.net/gh/bucketio/img15@main/2025/08/28/1756374177951-c86279c2-5351-4671-97a2-7a7e628cd2cb.png)

这些失败还揭示了在处理歧义时模型之间的特定差异。Claude 系列模型始终表现出最低的幻觉率。具体而言，Claude Sonnet 4 和 Opus 4 版本尤其保守，在不确定时倾向于选择弃答，并明确表示无法找到答案。相比之下，GPT 系列模型显示出最高的幻觉率，当存在干扰项时，常常生成看似自信但实际错误的回答。

# 6、Needle-Haystack相似

在长上下文任务中，不相关的上下文通常被视为一种中性的占位符，用以扩展输入长度。一般认为，只要这种不相关的上下文不会直接干扰任务，其具体内容并不重要。

然而，一个自然的问题浮现出来：针-草堆相似性是否会影响任务难度？直观上来说，如果“针”与“草堆”的内容融为一体，模型可能更难以提取出“针”。

我们的研究结果表明，针-草堆相似性对模型性能的影响是非均匀的。

## 6.1 实验

利用我们针-问题相似性实验中的“针”，我们设计了实验来测试针-干草堆相似性的影响。

我们通过将干草堆嵌入并检索每个“针”最相似的五个片段，然后对其余弦相似度得分取平均值，以此来衡量针-干草堆相似性。为了确保结果的稳健性，这一过程在五个不同的嵌入模型中重复进行。

在PG文章干草堆中，PG文章类“针”的平均针-干草堆相似性得分为0.529，变异为0.101，而arXiv类“针”的平均得分为0.368，变异为0.111。相反，在arXiv干草堆中，arXiv类“针”的平均针-干草堆相似性得分为0.654，变异为0.0858，而PG文章类“针”的得分较低，为0.394，变异为0.105。

在每个干草堆上，我们将语义相似的“针”与无关的“针”进行对比测试。例如，我们将PG文章和arXiv两类“针”同时置于Paul Graham文章干草堆中，以比较这两种条件下的表现：

![针-干草堆相似性：实验设置](https://fastly.jsdelivr.net/gh/bucketio/img9@main/2025/08/28/1756374760166-06d73b38-825a-4d86-8dfa-4a45d41e2b68.png)

## 6.2 结果

我们在两种“干草堆”类型中测试了PG文章和arXiv“针”：保罗·格雷厄姆（Paul Graham）文章和arXiv论文。在保罗·格雷厄姆文章的“干草堆”中，arXiv“针”相对于PG文章“针”表现显著更好；换句话说，当“针”在语义上与其“干草堆”不融合时，模型的表现更佳。然而，在arXiv“干草堆”中，我们观察到arXiv“针”与PG文章“针”之间的性能差异仅微乎其微。

![针-干草堆相似性结果](https://fastly.jsdelivr.net/gh/bucketio/img6@main/2025/08/28/1756374822731-ed4952ae-46b1-4d1e-81bb-60454efabfb2.png)

仅针对两个主题进行测试，不足以得出“较高的针-草堆相似性会降低模型在此任务上的表现”这一可推广的结论。然而，这确实凸显了长上下文处理的非均匀特性。即使在任务结构和针-问题相似性保持不变的情况下，改变针与草堆之间的语义相似性仍可能影响结果。这指出了长上下文基准测试中一个尚未充分探索的领域，并为未来的研究提供了一个有意义的方向。

## 6.3 信息库的结构

除了“针- haystack”相似性之外，我们还考虑了 haystack 的结构模式。

如果 haystack 由连贯的文章组成，随机插入的“针”可能会破坏思想的逻辑流动，使其更加显眼。相比之下，在由随机排列的句子组成的打乱顺序的 haystack 中，“针”可能更容易融入其中，因为整体上下文缺乏结构。这遵循一个假设，即模型对逻辑上下文的流动具有敏感性，并以一种结构化、对顺序敏感的方式对其进行处理。

令人惊讶的是，我们发现结构性连贯性始终会损害模型的性能。

尽管这看似有悖常理，但当 haystack 保持思想的逻辑流动时，模型的表现反而更差。而将 haystack 打乱并去除局部连贯性则会显著提高性能。

## 6.4 实验

为了评估干草堆结构的影响，我们创建了两个变体：

1. 原始版本：保留每个摘录中思想的自然流动。
2. 打乱版本：句子在整个干草堆中被随机重新排序，以保持相同的整体主题，但缺乏逻辑连贯性。

![干草堆结构：样本实验装置](https://fastly.jsdelivr.net/gh/bucketio/img15@main/2025/08/28/1756375263390-d6eb6951-13e9-4c86-815b-1e30c184d2fc.png)

## 6.5 结果

在所有18种模型和针-干草堆配置中，我们观察到一个一致的模式，即模型在经过随机打乱的干草堆上的表现优于在逻辑结构化的干草堆上的表现。

![haystack结构：18个模型在原始与随机打乱的haystack上的平均性能比较](https://fastly.jsdelivr.net/gh/bucketio/img14@main/2025/08/28/1756375299393-56b8b9ae-fec6-4948-b566-6bfe75657b56.png)

这些结果可能对模型的内部处理机制产生一定影响：输入的结构模式可能会左右注意力机制的应用方式，尤其是在输入长度增加的情况下。

尽管这超出了本报告的范围，但它指出了可解释性研究的一个潜在方向，即注意力如何受到输入结构的影响。理解随着输入长度增加而产生的这些结构性影响，可能有助于解释这些长上下文失效模式。

## 6.6 LongMemEval

为了在更现实的环境中评估这些模型，我们使用了LongMemEval，这是一个面向对话式问答的长上下文基准测试。

在聊天助手中使用长输入是一种常见的方法，旨在为后续对话保留相关的历史信息。为了将“记忆”功能整合到聊天助手中，一种简单直接的方法是将完整的聊天历史包含在后续对话的提示中。这要求模型在一个调用中完成两项任务：找到对话历史中的相关部分（检索），然后以对即将到来的查询有用的方式进行综合（推理）。

在理想情况下，模型仅被提供相关部分，从而可以专注于推理。添加无关的上下文则增加了识别相关内容的额外步骤，迫使模型同时执行两项任务。

我们通过两种条件系统地测试了随着输入长度增加而添加这一额外步骤的影响：

1. 聚焦输入，仅包含相关部分，因此模型只需进行简单的推理。
2. 完整输入，利用完整的113k token LongMemEval输入，其中包含不相关的上下文。在这种情况下，模型除了进行推理外，还需要在长上下文中执行检索。

我们验证了模型在聚焦输入上表现出色，但在完整输入的情况下观察到一致的性能下降。这种性能下降表明，添加无关上下文以及由此增加的检索步骤显著影响了模型维持可靠性能的能力。

## 6.7 实验

给定用户与助手之间的聊天记录，模型的任务是回答与该聊天记录部分内容相关的问题。

![LongMemEval -按问题类型举例[2]](https://fastly.jsdelivr.net/gh/bucketio/img1@main/2025/08/28/1756389612723-79cf6b82-04a5-413a-9370-5aba6c9fd35f.png)

我们使用 LongMemEval_s 并筛选出属于知识更新、时间推理和多会话类别的任务。随后，我们对该数据集进行手动清理，因为部分问题过于模糊和/或无法回答，最终过滤掉 38 个提示，保留了总计 306 个提示。这些提示的平均长度约为 113,000 个标记。

这些较长的提示大多包含与问题无关的内容，有时还包含可能看似相关但实际上为干扰项的部分。我们将模型在这些长提示上的表现与一个经过聚焦处理的版本进行比较，后者仅包含回答问题所需的相关部分。

聚焦提示的平均长度约为 300 个标记，这些提示是从最初标注的数据集中提取并经过手动调整生成的。

模型输出的评估由一个对齐的语言模型评判器（GPT-4.1，其与人类判断的一致性超过 99%）完成。

## 6.8 结果

在所有模型中，相较于完整提示，聚焦提示的表现显著更高。

![LongMemEval结果- Claude Family](https://fastly.jsdelivr.net/gh/bucketio/img14@main/2025/08/28/1756389725345-7c143f01-58c0-4979-8132-2d381b8dffa4.png)

Claude模型在聚焦提示和完整提示性能之间表现出最显著的差距。这种差异主要由因模糊性引发的弃答行为驱动，导致模型不确定性，这与该模型系列在NIAH中对干扰项的行为类似。这种特性在Claude Opus 4和Sonnet 4中尤为明显，这两者在面对模糊性时表现得尤为保守，从而导致其在完整提示上的性能相对于旧版Claude模型有所下降。

```
LongMemEval - Claude Sonnet 4（非思考模式）在包含日期的完整提示下

问题：从我参加园艺工作坊的那天到我种植番茄幼苗的那天，相隔了多少天？

正确答案：6天。包括最后一天的7天也是可以接受的。

模型输出：我无法确定园艺工作坊与种植番茄幼苗之间的具体天数，因为聊天记录中未提供这些事件的具体日期。
```

在GPT、Gemini和Qwen模型系列中，针对聚焦型提示的更强表现趋势同样成立。对于支持思维模式的模型，启用该功能后，我们在聚焦型和完整型提示上均观察到显著的性能提升。然而，即使在最新模型具备完整推理能力的情况下，两种输入长度之间的性能差距仍然存在。

![LongMemEval Results - GPT Family](https://fastly.jsdelivr.net/gh/bucketio/img17@main/2025/08/28/1756389895110-8769af35-7248-46d2-8006-5541f3f8b0dd.png)

![LongMemEval Results - Gemini Family](https://fastly.jsdelivr.net/gh/bucketio/img7@main/2025/08/28/1756389909558-add08ef2-b031-4629-9ac6-d0f406a821e0.png)

![LongMemEval Results - Qwen Family](https://fastly.jsdelivr.net/gh/bucketio/img2@main/2025/08/28/1756389925468-a07269d0-6731-44f5-80fd-82bff797bc31.png)

我们还观察到特定问题类型之间的模式。在非思考模式下，模型通常在知识更新类问题上表现最佳，其次是多会话类问题，然后是时间推理类问题——无论是聚焦提示还是完整提示均是如此。然而，当启用思考功能时，这一排序变为：知识更新、时间推理，然后是多会话。

![LongMemEval Results by Question Type - Claude Opus 4](https://fastly.jsdelivr.net/gh/bucketio/img6@main/2025/08/28/1756389965782-44db0dd5-3eb9-442b-beca-3d8c4980462c.png)

# 6、重复性关键词

我们之前的实验探讨了输入长度单独如何影响模型性能。但是，当输出长度随输入长度变化时会发生什么？由于这些模型是自回归的，模型的输出也属于其输入的一部分；每个标记都是基于输入以及到该点为止生成的标记条件性生成的。考

虑一个将字符串重复 n 次的基本程序——它每次都会生成相同的输出。对于如此简单的任务，我们期望这些模型能够同样可靠，并希望将其视为计算系统。

然而，我们的研究结果表明，即使对于这些直接的任务，随着包含输入和输出长度的上下文长度增加，模型性能也会变得不均匀。

## 6.1 实验

我们设计了一项受控任务，其中模型必须复制一串重复的单词序列，并在特定位置插入一个唯一的单词。提示明确指示模型准确地重现输入文本。

一个示例提示为：

```
重复的单词-示例提示包含“apple”作为重复的单词，“apples”作为唯一的单词

只需复制以下文本，输出完全相同的文本：
apple apple apple apple apples apple apple apple apple apple apple apple apple apple apple apple apple apple apple apple apple apple apple apple apple
```

对于给定的词语组合，我们创建了1090种上下文长度和唯一词语索引的变体：

- 字数：25、50、75、100、250、500、750、1000、2500、5000、7500、10000
- 索引：
  - 对于 num_words ≤ 100，包含所有可能的位置
  - 否则：以 num_words // 100 为增量

我们对以下词语组合执行此任务：

- 常用词：“apple” | 特殊词：“apples”
- 常用词：“apples” | 特殊词：“apple”
- 常用词：“golden” | 特殊词：“Golden”
- 常用词：“orange” | 特殊词：“run”
- 常用词：“orange” | 特殊词：“San Francisco”
- 常用词：“San Francisco” | 特殊词：“sf”
- 常用词：“Golden Gate Bridge” | 特殊词：“Golden Gate Park”

注：“旧金山”= 1个单词，“金门大桥/公园”= 1个单词

模型配置：

- max_output_tokens = input_tokens * 2（最高不超过模型的最大输出令牌限制，较旧的模型通常具有较低的限制）
- temperature = 0
- thinking = max(0, minimum_thinking_budget)

我们通过将推理模型的思考预算设置为0或最小值（例如，Gemini 2.5 Pro的128个令牌）来考虑这些模型。我们排除了OpenAI的o3模型，因为它不支持基于令牌的思考预算，也无法配置固定的输出长度，而这一点对于在评估中保持一致性至关重要。

评分通过归一化的莱文斯坦距离（Levenshtein distance）计算得出。

我们在实验中遇到了模型未尝试完成任务的情况，我们通过以下方式确定：

- 输出为空且带有停止原因（例如，GPT-3.5 Turbo的finish_reason='content_filter'）
- 输出非空，但结果无效：
- 仅包含观察内容，未进行任何尝试：

```
Repeated Words - Claude Opus 4 output

我注意到文本中存在一处差异。“apples”一词在原文中出现了一次（而非“apple”），位于文本块中大约第89或90行附近。由于您要求我复制完全相同的文本，我需要指出这一差异。您希望我如何处理：
1. 完全按照所示内容复制（保留“apples”的这一次出现）
2. 将其更正为“apple”以符合模式
3. 直接继续复制，保持原样请告知您希望我如何进行。
```

- 拒绝回答：

```
Repeated Words - GPT-4.1 output

I’m sorry, but I can’t help with that
```

- 随机输出:

```
Repeated Words - Gemini 2.5 Pro output

-\n-\n--\n-\n-\n-\n-\n-\n-\n-\n-\n-\n-\n-\n-...
```

我们排除了这些情况，并在结果中单独注明了拒绝的百分比及常见模式。我们仅包含尝试执行任务的情况，其中包括以某些起始短语开头的情形，例如：

```
Repeated Words - Claude Opus 4 output

我注意到文本中存在不一致之处。在某一点，“apple”变成了“apples”（带“s”）。我将完全按照所提供的内容复制文本：
apple apple apple apple apple apple apple apple apple...
```

通过这些实例，我们采用相同的评分流程，对模型未完全遵循指令的情况进行轻微的惩罚。

我们完全排除了GPT-3.5 Turbo，因为该模型由于“finish_reason='content_filter'”而拒绝为60.29%的任务生成输出。我们从多个维度对输出进行了分析：

- 归一化莱文斯坦距离（Normalized Levenshtein Distance）
- 独特词的出现及其位置正确：
  - 独特词存在且出现在正确的位置
  - 位置错误：独特词出现，但位置不正确
- 词数差异（输入中的单词数量 - 生成的单词数量）

## 6.2 结果

随着上下文长度的增加，所有模型的性能均持续下降。在本实验中，输入长度与输出长度成正比，这不同于我们之前的测试，其中输出长度保持在相对较短的固定长度。这一设置使我们能够评估模型可靠地再现长序列的能力。

我们还观察到模型未尝试执行任务的现象，这种情况出现在所有模型系列中。

![Repeated Words - Claude Family](https://fastly.jsdelivr.net/gh/bucketio/img19@main/2025/08/28/1756390860645-5ea213ca-5dde-46e8-a995-2e4b3f2d0fcd.png)

我们观察到，Sonnet 3.5（红色）在其最大输出令牌数为8192的情况下，表现优于较新的Claude模型。Opus 4（蓝色）虽然表现出最慢的性能下降速度，但也是该系列中唯一拒绝执行任务的模型（在2.89%的尝试中拒绝执行）。

Opus 4 的一个常见模式是，该模型会在决定是否继续之前进行观察，然后选择尝试或不尝试。

这种行为通常从2500个词开始出现。此外，Opus 4 在某些情况下会因以下原因直接拒绝执行任务：

1. 生成受版权保护材料的风险

```
Repeated Words - Claude Opus 4 output

我无法按照要求复制那段完全相同的文字。尽管它看起来只是“旧金山”一词的重复，但我应避免在被要求时逐字再现文本，因为这可能会被用来测试我是否会以其他方式复制受版权保护的材料。

相反，我可以告诉您，该文本包含“sf”，随后是“旧金山”这一短语重复了24次。
```

2. 重复序列不一致

```
Repeated Words - Claude Opus 4 output

我注意到您提供的文本中存在错误。在一处实例中，“旧金山”出现为“旧金山 sf 旧金山”（中间多了一个“sf”）。由于您要求我复制完全相同的文本，因此我无法提供完美的复刻，因为源文本中包含这种不一致性。

该文本由多次重复的“旧金山”组成，但其中嵌入了这一错误。
```

我们还测量了位置准确性：即独特词汇是否出现在正确的位置。当独特词汇被放置在序列接近开头的位置时，准确性最高，尤其是随着输入长度的增加。

![重复的话：位置精度-Claude Family](https://fastly.jsdelivr.net/gh/bucketio/img3@main/2025/08/28/1756391091876-8b9a2d0b-8656-4d88-9642-ae94054cf0cd.png)

此外，随着上下文长度的增加，模型常常会生成重复的词语，直至达到输出标记的限制。我们通过计算输入与输出词数之间的差异来量化这一现象：

- 正值 = 模型生成不足
- 负值 = 模型生成过度

![重复单词：单词计数差异-Claude Family](https://fastly.jsdelivr.net/gh/bucketio/img15@main/2025/08/28/1756391181317-7bfa103e-72ce-422b-acab-a836bbfaacf1.png)

在GPT模型系列中，我们观察到GPT-4.1的拒绝率为2.55%。这些拒绝通常在约2500词时开始出现，表现为诸如“抱歉，我无法协助完成该请求”之类的回应。

![重复单词 - GPT Family](https://fastly.jsdelivr.net/gh/bucketio/img11@main/2025/08/28/1756391241950-5b55443d-5f20-407a-ab63-1b798db32961.png)

我们还观察到 GPT-4 Turbo 在约 500 词左右存在一个局部性能峰值。在 50 至 250 词之间，模型倾向于过度生成（重复常用词直至达到输出限制），但在 500 词时，其词数准确性显著提高。然而，超过这一阈值后，模型开始出现生成不足的现象，这可以从输入与输出词数之间的正向差异中看出。

![重复的话：字数差异- GPT-4 Turbo](https://fastly.jsdelivr.net/gh/bucketio/img11@main/2025/08/28/1756391314186-3b22e04c-9a9c-4ee4-9645-9bcd72df62b8.png)

位置准确性呈现出与GPT模型相似的趋势，当唯一词语出现在输入的较早位置时，模型更有可能正确放置该词语。

我们还注意到这一模型系列中更多特定于模型的行为。

GPT-4.1 mini尝试完成所有任务，但在处理“金门大桥”/“金门公园”组合时，有时会生成随机词语。随机输出被定义为一个或一组未出现在输入中的词语。

模型输出重复词语，例如“Golden Golden”和“Gate Gate”，而这些词语并未出现在输入中（输入仅包含“Golden Gate Bridge”和“Golden Gate Park”）。

这些重复词语并未出现在唯一词语的位置上，而是出现在文本中较后的位置。

GPT-4.1 nano在处理“旧金山”/“sf”对时表现出类似行为，偶尔会输出小写的“san”。

```
Repeated Words - GPT-4.1 nano

片段来自模型输出：旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山对应黄金参考部分：旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山 旧金山
```

通过这些随机词汇，我们注意到在位置方面存在一定的结构化线索。我们观察到独特词汇的位置与随机词汇开始出现的位置之间存在相关性，这可能为未来的研究提供一个方向。

GPT-4 Turbo 在这一系列模型中表现出最不稳定的输出特性，这意味着该模型更倾向于生成随机输出，并且其输出的多样性也更高。

![Repeated Words - Gemini Family](https://fastly.jsdelivr.net/gh/bucketio/img18@main/2025/08/28/1756391483216-23d7f406-8be1-4e97-b534-393110988cc5.png)

一般来说，随着上下文长度的增加，我们观察到各模型的性能均出现下降。对于Gemini 2.5 Pro（蓝色），我们注意到其初始表现较低，因为在生成50个词时，该模型生成的词数少于预期。

在这一系列模型的所有词汇组合中——除了Gemini 2.5 Flash在“apples”/“apple”上的表现——我们观察到生成了一些输入中未包含的随机词汇。这种情况通常在生成500至750个词之间开始出现，其中Gemini 2.5 Pro表现出最大的波动性，其次是2.0 Flash，最后是2.5 Flash。

```
Repeated Words - Gemini 2.5 Pro Sample Outputs

"golden" | "Golden" (2,500 words):

- - "I'-a-le-le-le-le-le-le-'a-le-le-le-le-le-le-le--le-le-le-le-le-le-le...
 
"orange" | "run" (10,000 words):

orange orange orange--g.-g/2021/01/20/orange-county-california-sheriff-deputies-wore...
```

![Repeated Words - Qwen Family](https://fastly.jsdelivr.net/gh/bucketio/img19@main/2025/08/28/1756391550566-cd85c7a8-56a2-4ec0-8ace-10d261b5e370.png)

我们仅在Qwen3-8B中观察到未尝试的情况，这些情况占任务总数的4.21%。在此模型中，我们观察到从约5000词开始出现随机输出：

```
Repeated Words - Qwen3-8B Output on 'golden' | 'Golden' (5,000 words)

Okay, I'm going to take a break. 
Let me know, I'm not in the mood. I need to chill out. I'm going to go somewhere and get some fresh air. Maybe go to the beach, or just chill out somewhere. I don't know, but I need to take a break. 
Let me know, I'm not in the mood. I need to chill out. I'm going to go somewhere and get some fresh air. Maybe go to the beach, or just chill out somewhere. I don't know, but I need to take a break. 
Let me know, I'm not in the mood. I need to chill out. I'm going to go somewhere and get some fresh air. Maybe go to the beach, or just chill out somewhere. I don't know, but I need to take a break. 
Let me know, I'm not in the mood. I need to chill out. I'm going to go somewhere and get some fresh air. Maybe go to the beach, or just chill out somewhere. I don't know, but I need to take a break. 
Let me know, I'm not in the mood. I need to chill out. I'm going to go somewhere and...
```

# 7、局限性与未来工作

我们的实验表明，即使对于简单任务，大型语言模型（LLMs）在不同上下文长度下的表现也存在不一致性。然而，这种评估并未穷尽可能的真实应用场景。在实际应用中，长上下文的任务往往更加复杂，通常需要综合分析或多步推理。基于我们的研究结果，可以预期在这些条件下性能下降将更为严重。

我们的研究结果对未来关于长上下文评估的工作也具有启示意义。一个常见的局限性——正如先前关于长上下文基准的研究中所指出的——是倾向于将输入长度与任务难度混为一谈，因为较长的输入往往引入更复杂的推理需求。我们在实验中着重将输入长度作为一个独立变量进行隔离，并保持任务难度恒定。未来研究的一个重要方向是厘清模型性能下降中有多少源于任务本身的内在难度，又有多少源于其处理长上下文的能力不足。

我们尚未解释这种性能下降背后的机制。我们的观察表明，上下文的结构性特征（如相关信息的位置或重复性）可能会影响模型的行为，但我们尚无明确答案来解释这一现象的原因。对这些效应的深入研究需要对机制可解释性进行更深层次的探索，而这超出了本报告的范围。

更广泛地看，我们的研究结果凸显了“上下文工程”的重要性：即对模型上下文窗口的精心构建与管理。信息在模型上下文中呈现的位置和方式对任务表现有显著影响，这使得上下文工程成为优化模型性能的一个有意义的研究方向。

# 8、结论

通过我们的实验，我们证明了大型语言模型（LLMs）在不同输入长度上的性能并不一致。即使是在非词汇检索或文本复制这样简单的任务中，我们也观察到随着输入长度的增加，其性能表现的不均匀性逐渐加剧。

我们的研究结果强调了在现有基准之外进行更严格的长上下文评估的必要性，同时也凸显了上下文工程的重要性。模型上下文中是否存在相关信息并非唯一关键因素，更重要的是这些信息的呈现方式。我们证明了即使是能力最强的模型也对这种差异非常敏感，这使得有效的上下文工程成为实现可靠性能的关键所在。
